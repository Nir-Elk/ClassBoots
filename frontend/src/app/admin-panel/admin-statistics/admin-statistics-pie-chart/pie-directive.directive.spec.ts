import { PieDirectiveDirective } from './pie-directive.directive';

describe('PieDirectiveDirective', () => {
  it('should create an instance', () => {
    //const directive = new PieDirectiveDirective();
    //expect(directive).toBeTruthy();
  });
});
